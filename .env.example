# Database — SQLite local (Prisma 7, driver adapter)
DATABASE_URL="file:./dev.db"

# URL pública para canonical/sitemap (sem barra final)
# NEXT_PUBLIC_APP_URL="https://radarbancario.com.br"

# IA opcional — habilita o modelo no /chat quando a base de conhecimento não responde
# OPENAI_API_KEY=""
# AI_BASE_URL="https://api.openai.com/v1"
# AI_MODEL="gpt-4o-mini"

# Protege o endpoint POST /api/cron/summary em produção
# CRON_SECRET=""
